const Config = {
  app: require("./app"),
  auth: require("./auth"),
  db: require("./db"),
  paths: require("./paths"),
  session: require("./session"),
  redis: require("./redis"),
};

module.exports = Config;
